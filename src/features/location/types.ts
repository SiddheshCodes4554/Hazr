export interface Coordinates {
  latitude: number;
  longitude: number;
}

export interface HazardLocation {
  latitude: number;
  longitude: number;
}

export interface ReporterLocation {
  latitude: number;
  longitude: number;
}
